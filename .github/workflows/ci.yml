# CI skeleton — add lint/test/deploy steps as needed
name: CI

on:
  push:
    branches: [main, develop, testing]
  pull_request:
    branches: [main, develop, testing]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "CI pipeline — add jobs here"
